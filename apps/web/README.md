# Web App (Next.js)

Operational UI for Quantum Control Plane.

## Views

- Experiment submission (`/experiments/new`)
- Run history (`/runs`)
- Run details (`/runs/[id]`)
- Provider comparison (`/comparison`)

## Commands

```bash
npm install
npm run dev
npm run lint
npm run typecheck
```
