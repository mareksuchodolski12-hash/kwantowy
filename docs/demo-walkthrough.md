# Demo Walkthrough

1. Bring stack up (`make up && make migrate`).
2. Open UI and submit one local run.
3. Submit one IBM run (if credentials configured).
4. Show run history and drill into run details.
5. Open comparison page and discuss provider-level behavior.
6. Show `/metrics`, Prometheus dashboard, and Grafana panel.
7. Show Helm chart + Terraform namespace baseline + CI workflow.
