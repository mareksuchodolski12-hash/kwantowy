apiVersion: v1
kind: Secret
metadata:
  name: qcp-secrets
  labels:
    app.kubernetes.io/part-of: quantum-control-plane
type: Opaque
stringData:
  database-url: {{ .Values.env.databaseUrl | quote }}
  redis-url: {{ .Values.env.redisUrl | quote }}
